// import { computePosition } from 'https://cdn.jsdelivr.net/npm/@floating-ui/dom@1.3.0/+esm';

// const popover = document.querySelector('[popover]');
// popover.addEventListener('toggle', positionPopover);

// function positionPopover(event) {
//   const invoker = document.querySelector(`[popovertarget="${popover.getAttribute('id')}"`);

//   if (event.newState === 'open') {
//     computePosition(invoker, popover, { placement: 'bottom-start' }).then(({x, y}) => {
//       Object.assign(popover.style, {
//         left: `${x}px`,
//         top: `${y}px`,
//       });
//     });
//   }
// }

{
  /* <p>This is a minimal example of positioning a <a href="https://html.spec.whatwg.org/dev/popover.html#the-popover-attribute">HTML popover</a> with <a href="https://floating-ui.com/">Floating UI</a>, as an example with the blog post <a href="https://hidde.blog/positioning-popovers">Positioning popovers on hidde.blog</a>.</p>
<p>Note: this works in browsers that support popover, the <a href="https://github.com/oddbird/popover-polyfill">polyfill</a> is included for others (remove to get a more minimal test environment).</p>
<button popovertarget="p">Toggle popover</button>
<div id="p" popover>… popover contents go here</div>

<script
  src="https://cdn.jsdelivr.net/npm/@oddbird/popover-polyfill@latest"
  crossorigin="anonymous"
  defer
></script>
<link
  rel="stylesheet"
  href="https://cdn.jsdelivr.net/npm/@oddbird/popover-polyfill@latest/dist/popover.css"
  crossorigin="anonymous"
/> */
}
